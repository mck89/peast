while (check() && !finished()) {
	doSomething();
}
/**************************************************/
while(check()&&!finished())doSomething();
/**************************************************/
while ( check(  ) && !finished(  ) )
{
	doSomething(  );
}