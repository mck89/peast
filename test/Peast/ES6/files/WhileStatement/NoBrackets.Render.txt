while (num in rows) {
	printRow(rows[num++]);
}
/**************************************************/
while(num in rows)printRow(rows[num++]);
/**************************************************/
while ( num in rows )
{
	printRow( rows[num++] );
}